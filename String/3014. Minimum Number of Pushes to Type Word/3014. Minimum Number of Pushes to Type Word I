class Solution:
    def minimumPushes(self, word: str) -> int:

        n = len(word)
        i = 1
        ans = 0
        while n >=0:
            left = n % 8 if n < 8 else 8
            n = n - 8
            ans = ans + left * i
            i += 1
        return ans

